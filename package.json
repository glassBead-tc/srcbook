{
  "name": "srcbook-monorepo",
  "private": true,
  "packageManager": "pnpm@9.7.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev:runner": "pnpm --filter @srcbook/runner dev",
    "start:mcp": "pnpm --filter @srcbook/mcp-gcp-sweeper start"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
